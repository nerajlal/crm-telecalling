<div class="table-wrap"><table><thead><tr><th>Lead</th><th>Employee</th><th>Customer outcome</th><th>Talk time</th><th>Called · IST</th>@unless($compact??false)<th>Call state</th>@endunless</tr></thead><tbody>@forelse($calls as $call)<tr><td><a class="table-link" href="{{ route('leads.show',$call->lead_id) }}">{{ $call->lead->name }}</a>@if($call->provider==='demo')<small class="demo-inline">Demo</small>@endif</td><td>{{ $call->employee->name }}</td><td><x-badge :value="$call->customer_status"/></td><td class="nowrap">{{ \App\Support\India::duration($call->duration_seconds) }}</td><td class="nowrap muted">{{ \App\Support\India::display($call->initiated_at,'d M, h:i A') }}</td>@unless($compact??false)<td>{{ ucfirst($call->status) }}<small>{{ $call->provider==='demo'?'Simulation':($call->isProviderVerified()?'Provider verified':'Unverified request') }}</small>@if($call->resolved_at)<small>Lock released</small>@endif</td>@endunless</tr>@empty<tr><td colspan="6" class="empty">No calls in this view yet.<br><small>Calls made from the CRM will appear automatically.</small></td></tr>@endforelse</tbody></table></div>
