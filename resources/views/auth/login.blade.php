<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Sign in · TeleCRM</title><link rel="icon" href="{{ asset('favicon.svg') }}">@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="login-body">
<section class="login-story"><a class="brand" href="{{ route('login') }}"><span class="brand-mark"><x-icon name="phone"/></span>tele<span class="brand-light">crm</span><span class="brand-dot">.</span></a><div class="login-copy"><span class="eyebrow">EVERY CONVERSATION COUNTS</span><h1>Your team.<br>Your leads.<br>One clear picture.</h1><p>Less time updating spreadsheets.<br>More time building relationships.</p><div class="login-proof"><span class="proof-icon"><x-icon name="check"/></span> Calls, follow-ups, and progress — together.</div></div><small>Built for your team in India.</small></section>
<section class="login-panel"><div class="login-box"><span class="eyebrow">WELCOME BACK</span><h2>Sign in to your workspace</h2><p class="muted">Pick up where your last conversation left off.</p>
@if($errors->any())<div class="alert error" role="alert">{{ $errors->first() }}</div>@endif
@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif
<form action="{{ url('/login') }}" method="post" class="stack">@csrf<label>Email address<input name="email" type="email" autocomplete="username" value="{{ old('email') }}" placeholder="you@company.in" required autofocus></label><label>Password<input name="password" type="password" autocomplete="current-password" placeholder="Enter your password" required></label><button class="button primary full" type="submit">Sign in <x-icon name="arrow" size="18"/></button></form><p class="login-help">Need access? Contact your workspace owner.</p>
@if(\App\Models\Call::demoEnabled())<div class="demo-login"><span class="demo-tag">DEMO WORKSPACE</span><p>Calls are simulated in this environment.<br>Use the demo credentials in the project README.</p></div>@endif
</div></section></body></html>
